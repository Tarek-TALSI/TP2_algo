#include "exo1.hpp"
#include <fstream>
#include <iostream>

vector<vector<int>> txt_to_matrix(const string& filename){
    ifstream fin(filename);
    vector<vector<int>> matrix;

    int x = 0;
    while(fin >> x){
        
    }
}